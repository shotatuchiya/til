確認ページを作ろう

ここからは以下の様な注文確認ページを作っていきます。

まずは、フォームから送信されたそれぞれのメニューの注文個数を受け取り、それを表示します。

送信された値を受け取る

フォームで送信された値を「$_POST」で受け取れることはPHPⅠで学習しました。

<input>タグのname属性はそれぞれのMenuインスタンスのnameプロパティなので、nameプロパティの値を用いて送信された値を受け取りましょう。
